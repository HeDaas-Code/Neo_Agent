# Cognee 智能记忆系统集成文档

## 概述

Neo Agent 集成了 [Cognee](https://docs.cognee.ai/) 作为智能记忆引擎，将原始数据（对话、文档、知识）转化为智能体的持久动态 AI 记忆。

## 什么是 Cognee？

Cognee 是一个开源的知识引擎，它结合了向量搜索和图数据库，使文档既可以按语义搜索，又能通过关系相互关联。

### 核心功能

- **持久动态记忆**: 将原始数据转化为可持久化的 AI 记忆
- **知识图谱**: 自动构建实体关系图谱
- **语义搜索**: 基于向量的含义检索
- **模块化知识**: 支持自定义知识块

## 安装配置

### 1. 安装依赖

Cognee 已包含在 `requirements.txt` 中：

```bash
pip install -r requirements.txt
```

或单独安装：

```bash
pip install cognee>=0.5.0
```

### 2. 配置环境变量

在 `.env` 文件中添加以下配置：

```env
# 是否启用 Cognee 记忆系统（默认True）
COGNEE_ENABLED=true

# LLM API 密钥（Cognee 使用 LLM 进行知识提取）
# 默认使用 SILICONFLOW_API_KEY
LLM_API_KEY=your-api-key
```

## 使用方式

### 1. 自动集成

当启用 Cognee 后，Neo Agent 会自动：

- 将对话消息同步到 Cognee 记忆系统
- 每 5 轮对话自动构建知识图谱
- 在知识提取时同步到 Cognee

### 2. GUI 管理

在数据库管理界面中，新增了两个标签页：

#### 🧠 Cognee 记忆

- **添加记忆**: 手动添加对话、知识、世界观记忆
- **搜索记忆**: 基于语义的智能搜索
- **统计信息**: 查看 Cognee 系统状态

#### 🌍 世界观构建

- **文件管理**: 管理 Markdown 世界观文件
- **编辑器**: 内置 Markdown 编辑器
- **AI 生成**: 自然语言生成世界观
- **同步功能**: 同步到 Cognee 和知识库

### 3. API 使用

```python
from src.core.cognee_memory import CogneeMemoryManager

# 创建管理器
manager = CogneeMemoryManager()

# 添加记忆（异步）
await manager.add_memory("用户: 你好\n助手: 你好！", "conversation")

# 添加知识
await manager.add_knowledge("小明", "用户的名字是小明", "fact")

# 构建知识图谱
await manager.cognify()

# 搜索记忆
results = await manager.search("小明")
```

### 4. 同步包装器（同步调用）

```python
from src.core.cognee_memory import get_cognee_sync_wrapper

wrapper = get_cognee_sync_wrapper()

# 同步添加记忆
wrapper.add_memory("内容", "conversation")

# 同步搜索
results = wrapper.search("查询")
```

## 记忆类型

Cognee 支持三种记忆类型：

| 类型 | 说明 | 用途 |
|------|------|------|
| `conversation` | 对话记忆 | 存储对话历史 |
| `knowledge` | 知识记忆 | 存储实体知识和事实 |
| `worldview` | 世界观 | 存储虚拟世界设定 |

## 世界观构建

### Markdown 世界观

世界观文件存储在 `prompts/worldview/` 目录，使用 Markdown 格式：

```markdown
# 我的世界观

## 世界基本信息

**世界名称**：魔法世界
**时代背景**：中世纪

## 世界特征

### 1. 魔法系统

这个世界存在魔法...
```

### AI 生成

使用自然语言描述生成世界观：

```python
from src.core.worldview_builder import WorldviewBuilder

builder = WorldviewBuilder()

# 自然语言生成
content = builder.create_worldview_from_natural_language(
    "一个充满魔法的中世纪世界，有精灵、矮人和人类共存",
    name="魔法世界",
    use_llm=True
)

# 保存
builder.save_worldview("magic_world", content)
```

### 同步到 Cognee

```python
# 同步世界观到 Cognee
await builder.sync_to_cognee("magic_world")

# 同步到传统知识库
builder.sync_to_knowledge_base("magic_world")
```

## 与现有系统集成

### LongTermMemoryManager

`LongTermMemoryManager` 已集成 Cognee：

```python
from src.core.long_term_memory import LongTermMemoryManager

# 自动启用 Cognee
manager = LongTermMemoryManager(enable_cognee=True)

# 添加消息时自动同步到 Cognee
manager.add_message("user", "你好")

# 搜索 Cognee 记忆
results = manager.search_cognee_memory("你好")
```

### 统计信息

```python
stats = manager.get_statistics()
print(stats['cognee'])  # Cognee 统计信息
```

## 架构图

```
┌─────────────────────────────────────────────────────┐
│                   Neo Agent                          │
├─────────────────────────────────────────────────────┤
│                                                      │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────┐  │
│  │  对话系统   │───▶│ 长期记忆管理 │───▶│ Cognee  │  │
│  └─────────────┘    └─────────────┘    └─────────┘  │
│         │                  │                 │       │
│         ▼                  ▼                 ▼       │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────┐  │
│  │  知识提取   │───▶│  知识库     │◀──▶│ 知识图谱 │  │
│  └─────────────┘    └─────────────┘    └─────────┘  │
│                                                      │
│  ┌─────────────────────────────────────────────────┐ │
│  │              世界观构建系统                      │ │
│  │  ┌───────────┐    ┌───────────┐    ┌─────────┐  │ │
│  │  │ Markdown  │───▶│ 模块解析   │───▶│ Cognee  │  │ │
│  │  │   文件    │    │           │    │  同步   │  │ │
│  │  └───────────┘    └───────────┘    └─────────┘  │ │
│  └─────────────────────────────────────────────────┘ │
│                                                      │
└─────────────────────────────────────────────────────┘
```

## 注意事项

1. **API 密钥**: Cognee 需要 LLM API 密钥进行知识提取
2. **异步操作**: Cognee 核心操作是异步的，提供了同步包装器
3. **性能**: 知识图谱构建可能需要一定时间
4. **存储**: Cognee 使用本地存储，数据保存在当前目录

## 故障排除

### Cognee 未初始化

```
⚠ Cognee 初始化失败，将仅使用数据库存储
```

检查：
1. cognee 是否正确安装
2. LLM API 密钥是否配置
3. 网络连接是否正常

### 导入错误

```
⚠ Cognee 模块导入失败
```

解决方案：
```bash
pip install cognee>=0.5.0
```

## 参考链接

- [Cognee 官方文档](https://docs.cognee.ai/)
- [Cognee GitHub](https://github.com/topoteretes/cognee)
- [Neo Agent 主文档](../README.md)
