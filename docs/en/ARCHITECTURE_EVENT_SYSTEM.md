# Event-Driven System Architecture

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      User Interface Layer (GUI)                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │          gui_enhanced.py - Event Management UI           │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐         │   │
│  │  │Create Event│  │Trigger Event│  │View Details│         │   │
│  │  └────────────┘  └────────────┘  └────────────┘         │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       Agent Layer (ChatAgent)                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    chat_agent.py                          │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │  handle_event()  - Unified event processing entry │  │   │
│  │  │  ├─ process_notification_event()  Notification    │  │   │
│  │  │  └─ process_task_event()         Task             │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                Event Processing Layer (Coordinators)              │
│                                                                   │
│  ┌─────────────────────────┐     ┌──────────────────────────┐   │
│  │  Notification Handler   │     │   Task Event Handler     │   │
│  │  ┌──────────────────┐   │     │  multi_agent_coordinator │   │
│  │  │ 1. Understand    │   │     │  ┌───────────────────┐  │   │
│  │  │ 2. Generate Expl │   │     │  │ 1. Understand Task│  │   │
│  │  │ 3. Return Result │   │     │  │ 2. Make Plan      │  │   │
│  │  └──────────────────┘   │     │  │ 3. Execute Steps  │  │   │
│  └─────────────────────────┘     │  │ 4. Verify Result  │  │   │
│                                   │  └───────────────────┘  │   │
│                                   └──────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Tools Layer                               │
│  ┌──────────────────────────┐     ┌─────────────────────────┐   │
│  │ interrupt_question_tool  │     │    SubAgent (Agents)    │   │
│  │  ┌──────────────────┐    │     │  ┌──────────────────┐  │   │
│  │  │ ask_user()       │    │     │  │ Understanding    │  │   │
│  │  │ Format question  │    │     │  │ Planning         │  │   │
│  │  │ Callback handler │    │     │  │ Execution        │  │   │
│  │  └──────────────────┘    │     │  │ Verification     │  │   │
│  └──────────────────────────┘     │  └──────────────────┘  │   │
│                                   └─────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Data Storage Layer (Database)                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                 event_manager.py                          │   │
│  │  ┌────────────────┐         ┌─────────────────────────┐  │   │
│  │  │  events table   │         │   event_logs table      │  │   │
│  │  │  ├─ event_id   │         │   ├─ log_id             │  │   │
│  │  │  ├─ title      │         │   ├─ event_id           │  │   │
│  │  │  ├─ type       │◄────────┤   ├─ log_type           │  │   │
│  │  │  ├─ priority   │         │   ├─ log_content        │  │   │
│  │  │  ├─ status     │         │   └─ created_at         │  │   │
│  │  │  └─ metadata   │         └─────────────────────────┘  │   │
│  │  └────────────────┘                                       │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## Notification Event Processing Flow

```
User (GUI)
    │
    │ 1. Create notification event
    ▼
EventManager
    │
    │ 2. Save to database
    ▼
[events table]
    │
    │ 3. User triggers event
    ▼
ChatAgent.handle_event()
    │
    │ 4. Identified as notification
    ▼
ChatAgent.process_notification_event()
    │
    │ 5. Build understanding prompt
    ▼
SiliconFlowLLM
    │
    │ 6. Call API to understand event
    ▼
Agent analysis
    │
    │ 7. Generate explanation text
    ▼
ChatAgent
    │
    │ 8. Record log
    ▼
EventManager.add_event_log()
    │
    │ 9. Update status to completed
    ▼
EventManager.update_event_status()
    │
    │ 10. Return explanation to user
    ▼
GUI displays result
```

## Task Event Processing Flow

```
User (GUI)
    │
    │ 1. Create task event
    ▼
EventManager
    │
    │ 2. Save task information
    ▼
[events table]
    │
    │ 3. User triggers event
    ▼
ChatAgent.handle_event()
    │
    │ 4. Identified as task
    ▼
ChatAgent.process_task_event()
    │
    │ 5. Prepare role context
    ▼
MultiAgentCoordinator.process_task_event()
    │
    │ 📢 Progress: "Agent begins analyzing task..."
    ├──────────────────────────────────────┐
    │                                      │
    ▼ Phase 1: Task Understanding          │
SubAgent (Understanding Agent)             │
    │                                      │
    │ Analyze task requirements            │
    │ Understand completion criteria       │
    ▼                                      │
Return task understanding                  │
    │                                      │
    │ 📢 Progress: "Task understood..."    │
    ▼                                      │
    │                                      │
    ▼ Phase 2: Make Plan                   │
SubAgent (Planning Agent)                  │
    │                                      │
    │ Decompose task into 3-5 steps        │
    ▼                                      │
Return execution plan                      │
    │                                      │
    │ 📢 Progress: "Plan completed, N steps..."│
    ▼                                      │
    │                                      │
    ▼ Phase 3: Execute Steps               │
┌──────────────────┐                      │
│ FOR each step    │                      │
│   │              │                      │
│   ▼              │                      │
│ SubAgent (Exec)  │                      │
│   │              │                      │
│   │ Execute step │                      │
│   ▼              │                      │
│ Need user input? │                      │
│   │              │                      │
│   ├─ Yes ──────► │                      │
│   │  InterruptQuestionTool              │
│   │    │                                │
│   │    │ ask_user()                     │
│   │    ▼                                │
│   │  GUI dialog                         │
│   │    │                                │
│   │    │ User input                     │
│   │    ▼                                │
│   │  Return answer                      │
│   │    │                                │
│   ◄────┘                                │
│   │              │                      │
│   │ 📢 Progress: "Step N complete"     │
│   ▼              │                      │
│ Next step        │                      │
└──────────────────┘                      │
    │                                      │
    │ 📢 Progress: "All steps completed..."│
    ▼                                      │
    │                                      │
    ▼ Phase 4: Verify Result               │
SubAgent (Verification Agent)              │
    │                                      │
    │ Check against criteria               │
    │ Verify execution results             │
    ▼                                      │
Return verification result                 │
    │                                      │
    │ 📢 Progress: "✅ Task verified!"     │
    ▼                                      │
MultiAgentCoordinator                      │
    │                                      │
    │ Compile results                      │
    ▼                                      │
ChatAgent                                  │
    │                                      │
    │ Record logs                          │
    │ Update status                        │
    ▼                                      │
GUI displays result                        │
```

## Data Flow Diagram

```
┌──────────┐
│   User   │
└─────┬────┘
      │
      │ Create/Trigger event
      ▼
┌──────────────┐      ┌──────────────┐
│     GUI      │◄────►│ EventManager │
└──────┬───────┘      └──────┬───────┘
       │                     │
       │ Event operations    │ CRUD ops
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│  ChatAgent   │      │   Database   │
└──────┬───────┘      │  ├─ events   │
       │              │  └─ logs     │
       │ Processing   └──────────────┘
       │
       ├─────────────┐
       │             │
       ▼             ▼
  ┌─────────┐  ┌────────────────┐
  │Notif.   │  │ Multi-Agent    │
  │Handler  │  │  Coordinator   │
  └────┬────┘  └────────┬───────┘
       │                │
       │                ├─────────────┐
       │                │             │
       │                ▼             ▼
       │         ┌──────────┐  ┌─────────────┐
       │         │ SubAgent │  │ QuestionTool│
       │         └─────┬────┘  └──────┬──────┘
       │               │              │
       └───────────────┴──────────────┘
                       │
                       │ API calls
                       ▼
               ┌───────────────┐
               │ SiliconFlow   │
               │     API       │
               └───────────────┘
```

## Class Relationship Diagram

```
┌────────────────────┐
│       Event        │ Base event class
│  ──────────────    │
│  + event_id        │
│  + title           │
│  + description     │
│  + event_type      │
│  + priority        │
│  + status          │
│  + metadata        │
│  ──────────────    │
│  + to_dict()       │
│  + from_dict()     │
└─────────┬──────────┘
          │
          ├────────────────────┬──────────────────────┐
          │                    │                      │
┌─────────▼─────────┐  ┌───────▼────────┐  ┌────────▼─────────┐
│NotificationEvent  │  │   TaskEvent    │  │  (Extensible)    │
│                   │  │ ───────────    │  │                  │
│ Notification type │  │ + task_req     │  │  Other event     │
│                   │  │ + completion   │  │  types           │
└───────────────────┘  └────────────────┘  └──────────────────┘

┌────────────────────┐
│   EventManager     │ Event manager
│  ──────────────    │
│  + db              │
│  ──────────────    │
│  + create_event()  │
│  + get_event()     │
│  + update_status() │
│  + add_log()       │
│  + get_logs()      │
│  + delete_event()  │
│  + get_statistics()│
└────────────────────┘

┌────────────────────┐
│MultiAgentCoord.    │ Multi-agent coordinator
│  ──────────────    │
│  + question_tool   │
│  + progress_cb     │
│  ──────────────    │
│  + process_task()  │
│  + emit_progress() │
│  + _understand()   │
│  + _plan()         │
│  + _execute()      │
│  + _verify()       │
└────────────────────┘

┌────────────────────┐
│     SubAgent       │ Sub-agent
│  ──────────────    │
│  + agent_id        │
│  + role            │
│  + description     │
│  ──────────────    │
│  + execute_task()  │
└────────────────────┘

┌────────────────────┐
│InterruptQuestion   │ Interrupt question tool
│      Tool          │
│  ──────────────    │
│  + callback        │
│  ──────────────    │
│  + ask_user()      │
│  + _format_q()     │
└────────────────────┘
```

## State Machine Diagram

```
Event lifecycle state transitions:

         [Create]
            │
            ▼
    ┌──────────────┐
    │   PENDING    │ Pending
    └───────┬──────┘
            │ Trigger
            ▼
    ┌──────────────┐
    │  PROCESSING  │ Processing
    └───────┬──────┘
            │
        ┌───┴───┐
        │       │
    Success │   │ Failure
        │       │
        ▼       ▼
  ┌──────┐  ┌──────┐
  │COMPL │  │FAILED│
  │ETED  │  │      │
  └──────┘  └──────┘
  
  Can transition to from any state:
  ┌───────────┐
  │ CANCELLED │ Cancelled
  └───────────┘
```

## Module Dependencies

```
gui_enhanced.py
    ├─► chat_agent.py
    │       ├─► event_manager.py
    │       │       └─► database_manager.py
    │       ├─► interrupt_question_tool.py
    │       └─► multi_agent_coordinator.py
    │               ├─► event_manager.py
    │               ├─► interrupt_question_tool.py
    │               └─► debug_logger.py
    └─► event_manager.py
```

## Key Design Patterns

### 1. Strategy Pattern
- Different event types use different processing strategies
- `NotificationEvent` → `process_notification_event()`
- `TaskEvent` → `process_task_event()`

### 2. Chain of Responsibility Pattern
- Task passing in multi-agent collaboration
- Understanding → Planning → Execution → Verification

### 3. Observer Pattern
- Progress callback mechanism
- Narrative-style progress output

### 4. Factory Pattern
- `EventManager.create_event()` creates different events based on type

### 5. Context Manager Pattern
- Database connection management
- `with self.db.get_connection() as conn:`

---

Last Updated: 2025-01-19
